# AWS Bedrock Claude API key (you already have this from earlier work)
# If using Claude via LangChain, ensure ANTHROPIC_API_KEY or AWS credentials are set
ANTHROPIC_API_KEY=your_key_here

# Optional: for debugging
DEBUG=False
